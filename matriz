P= input()
P=int(P)
linhas = 10
colunas = 10
matriz = [] 
for l in range(linhas):
     lista = [int(x) for x in input().split()]
     matriz.append(lista)
X=0   
for l in range(linhas):     
    for c in range(colunas):
        if matriz[l][c]==P:
             X+=1 
        else:
            X+=0     
if X>=1:
    print("sim")
else:
    print("nao")
